import React from 'react';
import ReactDOM from 'react-dom';
import RestExample from "./Rest";

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
  <React.StrictMode>
    <RestExample/>
  </React.StrictMode>
);